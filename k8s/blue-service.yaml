apiVersion: v1
kind: Service
metadata:
  name: blue-service
  namespace: blue-green
spec:
  type: ClusterIP
  selector:
    app: flask-app
    version: blue
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
